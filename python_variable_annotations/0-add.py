#!/usr/bin/env python3
"""
This module contains a function to add two numbers.

The function 'add' takes two parameters, both integers or floats,
and returns their sum as a result.
"""


def add(a: float, b: float) -> float:
    """Adds two numbers and returns the sum."""
    return a + b
